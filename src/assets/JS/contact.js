/*const emailContact = document.querySelector('#email-contact');
const errorEmailContact = document.querySelector('.error-email-contact');
const form = document.querySelector('form.contact');



form.addEventListener('submit', (e) => { // Ajoute l'événement du clic sur le bouton Soumettre (son action étant par défaut 'submit').
    if (emailContact.value.includes('@yahoo')) { // Si le champ de l'adresse courriel contient //@yahoo//.
        e.preventDefault(); // Prévient l'action (événement) du bouton Soumettre si yahoo est inscrit dans le champ.
        errorEmail.textContent = "Erreur"; // Affiche le message d'erreur sous le champ Adresse courriel.
        emailContact.focus(); // Dirige l'utilisateur vers le champ fautif, soit le champ Adresse courriel, afin qu'il puisse corriger son adresse.
    } else {
    errorEmailContact.textContent = ''; // Vide le message d'erreur, spécialement dans le cas où il a été affiché (par défaut, il n'est pas affiché).
    }
});*/